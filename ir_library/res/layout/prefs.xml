<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    android:title="Intent Radio Preferences">

  <PreferenceCategory
     android:title="Notification">

     <CheckBoxPreference
       android:key="persistent_notification"
       android:title="Persistent notification"
       android:summary="Should the notification persist after playback completes?"
       android:defaultValue="true"
       />

  </PreferenceCategory>

  <PreferenceCategory
     android:title="Playback">

     <CheckBoxPreference
       android:key="reconnect"
       android:title="Restart playback"
       android:summary="If the network connection is lost then re-established during playback, then should playback be restarted? Playback is restarted only if the connection is re-established within five minutes."
       android:defaultValue="false"
       />

  </PreferenceCategory>

</PreferenceScreen>
